# Render detects this file to confirm it's a Python project.
# Dependencies are installed via pyproject.toml using the build command:
#   pip install -e ".[gate,api]"
fastapi>=0.110
uvicorn[standard]>=0.29
